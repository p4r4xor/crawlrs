[package]
name        = "crawlrs-fakes"
description = "Test doubles (fakes / stubs / in-memory impls) for crawlrs ports: FakeFetcher, InMemoryStore, InMemoryMetadataStore. Used as a dev-dependency by crates that test runtime composition without real I/O backends."
version.workspace    = true
edition.workspace    = true
license.workspace    = true
repository.workspace = true
authors.workspace    = true

[dependencies]
crawlrs-core.workspace = true

async-trait.workspace = true
bytes.workspace       = true
chrono.workspace      = true
smallvec.workspace    = true

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt"] }

[lints]
workspace = true
